Lock the exact successful 66-row v0.25 forward artifact from run 32602423009 into the append-only repository snapshot registry.
Retry after making the bot push rebase-safe; forecasting data and artifact are unchanged.
