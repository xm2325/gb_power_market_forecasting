# Test fixtures

`neso_*_official_sample.json` files are small snapshots from official NESO public resources and are committed only for schema/time-semantics tests.

`elexon_market_index_sample.json` and `elexon_system_price_sample.json` are **synthetic contract fixtures**. They test parser and join behaviour only; their values and derived metrics are not market evidence.
